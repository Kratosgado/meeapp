server.port=9000
spring.application.name=meeapp

# Standard Datasource config
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

security.jwt.secret-key=${JWT_SECRET_KEY}
security.jwt.expiration-time=${JWT_EXPIRATION_TIME:36000000}

# Hibernate
# spring.jpa.hibernate.ddl-auto=<create | create-drop | update | validate | none>
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.use-new-id-generator-mappings=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.default_schema=app_schema
# spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

# liquidbase properties
spring.liquibase.enabled=false
spring.liquibase.url=${spring.datasource.url}
spring.liquibase.user=${spring.datasource.url}
spring.liquibase.password=${spring.datasource.password}
spring.liquibase.change-log=filesystem:gradle/liquibase/changelog/root-changelog.sql
spring.liquibase.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.liquibase.diffChangeLogFile=gradle/liquibase/changelog/changes/V${version}__${description}.sql
spring.liquibase.diffTypes=tables,columns,indexes,foreignkeys,primarykeys,uniqueconstraints
# spring.liquibase.locations=classpath:db/migrations/postgresql
# spring.liquibase.default-schema=public
# spring.liquibase.liquidbase-schema=public
spring.liquibase.parameters.environment=dev
# spring.liquibase.driver-class-name=org.postgresql.Driver

# application.properties
# spring.jpa.properties.javax.persistence.schema-generation.scripts.action=create
# spring.jpa.properties.javax.persistence.schema-generation.scripts.create-target=src/main/resources/db/changelog/auto-generated.sql
